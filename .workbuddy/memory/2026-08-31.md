# 2026-08-31

## Project1（Cocos Creator 3.8.8 跑酷原型）进展

- 用户确认：wall 作为**终点判定**；相机 2 倍速问题已自行修掉（之前我们讨论过父子节点 setPosition 叠加，他重做了）。
- 碰撞方案从 `onCollisionEnter` 改为 **`onTriggerEnter`**（Car.ts 现在用 `onTriggerEnter` + `Move=false` + TextBox 显示"成功了/失败了" + 停止音效）。
- 已准备上传 GitHub：项目根目录已有 `.gitignore`，今日补全为 Cocos 标准版（排除 library/ temp/ local/ build/ native/ profiles/ + *.log + 系统/IDE 垃圾），保留 assets/、settings/、package.json、tsconfig.json。
- 帮用户准备面试话术：触发器 vs 碰撞器区别（onTriggerEnter=重叠检测、无物理响应；onCollisionEnter=实体碰撞、有反弹/阻挡；trigger 需 Collider.isTrigger=true 且成对物体至少一侧有 RigidBody）。
- 目标：把项目写进简历（定位为 Personal Project）。已给中/英版简历条目与强化建议（上 GitHub+README+动图、补完玩法、修 2x 相机等）。

## 待办 / 注意
- trigger 要真触发仍需：wall 的 Collider `isTrigger=true` + Car 节点 `RigidBody`（Kinematic，关重力）。否则 onTriggerEnter 仍不回调。
- `otherCollider.node.name == "wall"` 判断：wall 的子节点是 wall-left/wall-right，注意实际碰撞到的节点名是否真是 "wall"，否则可能恒走"失败了"分支。

## 今日追加：生成 README
- 已为项目根目录生成 `README.md`（中文为主、含英文对照）：含技术栈、功能特性、操作说明、运行方式、项目结构、核心实现说明、TODO、许可。
- 读 `Car.scene` 核对后发现的**上传前隐患（已口头提醒用户）**：
  1. 相机 2 倍速：场景里 `Main Camera`(节点5) 父节点仍是 `Car`(121)，而 `Car.ts` 第80-81行仍同时 `setPosition` 移动 Car 与相机（局部坐标会叠加）→ 2 倍速逻辑实际仍在，与"已修掉"说法不符。需把相机移出 Car 或改 `setWorldPosition`。
  2. trigger 触发前置：`wall` 的 BoxCollider 需 `isTrigger=true` 且碰撞对一侧有 RigidBody；场景里 RigidBody 在 `car`(122) 子节点与 `lun*` 车轮上，Car 主节点(121) 本体是否同时具备 Collider+RigidBody 需确认。
  3. 名字匹配：`Start_Collider` 用 `C.otherCollider.node.name == "wall"` 严格匹配，注意实际碰撞节点名（Wall/wall/wall-left/right）是否精确等于 "wall"。
